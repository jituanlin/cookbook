/**
 * BigInt is a built-in object that provides a way to represent whole numbers larger than 253 - 1.
 * typeof BigInt('1') === 'bigint'.
 * Can not use with method of `Math` or *normal* number.
 * Convert bigint to number will lose precision.
 * With BigInt, integer overflow () will no longer be an issue.
 * */
